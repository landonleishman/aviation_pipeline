services:
  db:
    image: postgres:15
    container_name: aviation-db
    environment:
      POSTGRES_USER: landon
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: aviation_pipeline
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: